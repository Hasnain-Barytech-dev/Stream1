# GCP Project Settings
# -------------------
# Your Google Cloud Platform project ID
GCP_PROJECT_ID=your-project-id-here
# The GCP region where your resources are deployed
GCP_REGION=us-central1

# Storage buckets
# --------------
# Bucket for storing original uploaded video files
RAW_VIDEOS_BUCKET=your-raw-videos-bucket
# Bucket for storing processed/converted video files
PROCESSED_VIDEOS_BUCKET=your-processed-videos-bucket

# GCP credentials - SENSITIVE
# --------------------------
# Your Google Cloud Storage access key (keep this secret!)
GCP_ACCESS_KEY=
# Your Google Cloud Storage secret key (keep this secret!)
GCP_SECRET_KEY=
# The endpoint URL for Google Cloud Storage
GCP_ENDPOINT_URL=https://storage.googleapis.com

# GCP Service Account Credentials JSON
# ----------------------------------
# Your service account JSON credentials (keep this secret and never commit to version control!)
# Either use this environment variable for the full JSON or use GOOGLE_APPLICATION_CREDENTIALS to point to a file
GCP_SERVICE_ACCOUNT_JSON=

# Alternative credentials approach (preferred)
# ------------------------------------------
# Path to your service account key file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Cloud Functions
# --------------
# Name of your video processing Cloud Function
VIDEO_PROCESSING_FUNCTION=process-video
# Name of your thumbnail generation Cloud Function
THUMBNAIL_GENERATION_FUNCTION=generate-thumbnails

# Django integration
# ----------------
# URL of your Django backend API
DJANGO_API_URL=http://django-backend:8080/api/v2

# JWT settings
# -----------
# Secret key for JWT token generation and validation (keep this secret!)
JWT_SECRET_KEY=your-super-secret-key-here
# Algorithm used for JWT tokens
JWT_ALGORITHM=HS256
# JWT token expiration time in minutes
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=180

# Development mode
# --------------
# Set to true for development environment, false for production
DEV_MODE=true

# Redis settings
# ------------
# Redis server hostname
REDIS_HOST=redis
# Redis server port
REDIS_PORT=6379
# Redis database number
REDIS_DB=0
# Redis password (if required)
REDIS_PASSWORD=

# FFmpeg settings
# -------------
# Number of threads for FFmpeg processing
FFMPEG_THREADS=4